name: speedx
base: core22
version: '2026.7.2'
summary: Realtime internet speed monitor
description: A linux based desktop application for Monitoring Internet speed with system monitor in cool graphics.
grade: stable
confinement: strict

icon: snap/gui/icon.png

architectures:
  - build-on: [amd64]

environment:
  SNAP_DESKTOP_RUNTIME: $SNAP/pyqt5-runtime-core22-lite

apps:
  speedx:
    command: bin/desktop-launch python3 $SNAP/speedx.py
    plugs:
      - home
      - gsettings
      - network
      - network-bind
      - desktop
      - desktop-legacy
      - x11
      - unity7
      - wayland
      - opengl
      - hardware-observe
      - network-status
    desktop: speedx.desktop
    environment:
      DISABLE_WAYLAND: 1

plugs:
  gsettings:
  gtk-3-themes:
    interface: content
    target: $SNAP/data-dir/themes
    default-provider: gtk-common-themes
  icon-themes:
    interface: content
    target: $SNAP/data-dir/icons
    default-provider: gtk-common-themes
  sound-themes:
    interface: content
    target: $SNAP/data-dir/sounds
    default-provider: gtk-common-themes
  pyqt5-runtime-core22-lite:
    interface: content
    target: $SNAP/pyqt5-runtime-core22-lite
    default-provider: pyqt5-runtime-core22-lite


parts:
  desktop-gui:
    plugin: dump
    source: src/
    override-build: |
      snapcraftctl build
      sed -i 's|Icon=.*|Icon=${SNAP}/meta/gui/icon.png|g' ${SNAPCRAFT_PART_SRC}/speedx.desktop
      mkdir -p ${SNAPCRAFT_PART_INSTALL}/usr/share/applications/
      cp -rf ${SNAPCRAFT_PART_SRC}/speedx.desktop ${SNAPCRAFT_PART_INSTALL}/usr/share/applications/speedx.desktop


  snap-launcher:
    plugin: dump
    source: snap_launcher/

  speedx:
      plugin: python
      source: .
      python-packages:
        - psutil
        - py-cpuinfo
        - cryptography
        - requests